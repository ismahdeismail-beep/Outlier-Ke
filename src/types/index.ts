export type Stat = {
  label: string;
  value: string;
};

export type Feature = {
  title: string;
  description: string;
  icon: string;
};

export type Job = {
  title: string;
  hourlyPay: string;
  isRemote: boolean;
};
